// Protected phrases that should never be split during word breaking.
// Add Khmer phrases as strings to prevent them from being segmented.
export const PROTECTED_PHRASES: string[] = [
  // "ព្រះជាម្ចាស់",
]
